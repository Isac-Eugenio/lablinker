name: lablinker
description: "APP BLUETOTH PARA O ARDUINO E ESP32"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter

  flutter_animate: ^4.5.2
  google_fonts: ^6.3.0
  provider: ^6.1.5
  signals: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - 'lib/app/shared/storage/assets/images/logo_lab.png'

